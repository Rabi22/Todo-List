<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>3D Todo List</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="box">
        <span>
            <h1>Todo List</h1>
        </span>
    </div>
    
    <div class="box">
        <% if (list.length === 0) { %>
            <div class="empty-state">
                <p>No tasks available. Add your first task below!</p>
            </div>
        <% } else { %>
            <ul>
                <% for(var i = 0; i < list.length; i++) { %>
                    <li>
                        <div class="task-checkbox" onclick="toggleTask(this)"></div>
                        <div class="task-text"><%= list[i] %></div>
                        <form action="/delete" method="POST" style="display: inline; margin: 0;">
                            <input type="hidden" name="taskIndex" value="<%= i %>">
                            <button type="submit" class="delete-btn">Delete</button>
                        </form>
                    </li>
                <% } %>
            </ul>
        <% } %>
        
        <form action="/" method="POST">
            <input type="text" name="ele1" placeholder="Add a new task..." required>
            <button type="submit">Add Task</button>
        </form>
    </div>

    <script src="/Tutedude/Todo_list/script.js"></script>
</body>
</html>